<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pendaftaranpasienpuskesmas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LihatPendaftar"></activity>
        <activity android:name=".DaftarPuskesmas" />
        <activity android:name=".Confirm" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Success" />
        <activity android:name=".Cek_Data_Diri" />
        <activity android:name=".Formulir_Pendaftaran" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>